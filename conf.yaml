openapi_path: example/github.json

api:
  base_url: https://api.github.com/
  max_rpm: 60
  request_timeout: 30
  retries:
    errors_max_retries: 3
    basic_retry_timeout: 2

database:
  connection_string: host=localhost user=postgres password=postgres dbname=postgres port=5440 sslmode=disable
